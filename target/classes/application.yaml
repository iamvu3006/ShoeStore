spring:
  application:
    name: shoestore

  datasource:
    url: jdbc:mysql://localhost:3306/shoe_store_db?createDatabaseIfNotExist=true
    username: root
    password: ""

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
